<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "add new dose", class: "btn btn-primary" %>
<% end %>

<%= link_to "Back to Cocktails", cocktail_path(@cocktail.id) %>
